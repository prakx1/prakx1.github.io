---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  activePath?: string;
}

const {
  title = "Notebook",
  description = "Notes on backend engineering, systems design, and things I am learning in public.",
  activePath = "/"
} = Astro.props;

const pageTitle = title === "Notebook" ? "Notebook" : `${title} · Notebook`;
const canonical = Astro.site ? new URL(Astro.url.pathname, Astro.site).toString() : Astro.url.pathname;

const nav = [
  { href: "/", label: "Home" },
  { href: "/about/", label: "About" },
  { href: "/projects/", label: "Projects" },
  { href: "/feed.xml", label: "RSS" }
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Prakash Singh" />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin="anonymous"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Instrument+Sans:wght@400;500;600;700&family=Syne:wght@600;700;800&family=IBM+Plex+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="backdrop" aria-hidden="true"></div>
    <div class="grid-overlay" aria-hidden="true"></div>
    <div class="app-shell">
      <header class="site-header">
        <a class="brand" href="/">
          <span class="brand-signal"></span>
          <span>
            <strong>Notebook</strong>
            <small>Prakash Singh</small>
          </span>
        </a>

        <nav>
          {nav.map((item) => (
            <a href={item.href} class={activePath === item.href ? "active" : ""}>{item.label}</a>
          ))}
        </nav>
      </header>

      <main>
        <slot />
      </main>

      <footer class="site-footer">
        <p>Writing in public about backend engineering and practical systems work.</p>
        <div>
          <a href="https://github.com/prakx1" target="_blank" rel="noopener noreferrer">GitHub</a>
          <a href="mailto:prakashsinghcs100@gmail.com">Email</a>
        </div>
      </footer>
    </div>
  </body>
</html>
